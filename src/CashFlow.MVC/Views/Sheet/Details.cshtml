@model CashFlow.Application.DTOs.SheetDTO

<h2>Sheet Details</h2>

<div>
    <h4>Sheet</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>

<h4>Entradas</h4>
@if (Model.Entries != null && Model.Entries.Any())
{
    <table class="table">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Valor</th>
                <th>Categoria</th>
                <th>Data</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var entry in Model.Entries)
            {
                <tr>
                    <td>@entry.Name</td>
                    <td>@entry.Value</td>
                    <td>@entry.Category</td>
                    <td>@entry.EntryDate</td>
                    <td>
                        <!-- Links para ações de Entry -->
                        <a asp-action="Details" asp-controller="FinancialEntry" asp-route-id="@entry.Id">Detalhes</a> |
                        <a asp-action="Edit" asp-controller="FinancialEntry" asp-route-id="@entry.Id">Editar</a> |
                        <a asp-action="Delete" asp-controller="FinancialEntry" asp-route-id="@entry.Id">Excluir</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Nenhuma entrada encontrada.</p>
}

<p>
    <a asp-action="Index">Voltar para a Lista</a> |
    <a asp-action="Create" asp-controller="FinancialEntry" asp-route-sheetId="@Model.Id">Criar Nova Entrada</a>
</p>
